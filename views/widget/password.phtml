<?php
/**
 * Never rendered with a value. A password that came back in the HTML would sit in the browser's
 * cache, in the page source and in anything that saves a copy of the page.
 */
$id = $form->idByNameAndField($name, $field);
?>
<input id="<?= $id ?>" name="<?= $form->inputName($name) ?>" type="password" value=""<?= esc_attrs($field['attributes'] ?? []) ?>>
