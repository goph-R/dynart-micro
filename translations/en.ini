; Built in messages of the framework.
;
; Register this folder in your application to get them translated:
;
;   Micro::get(TranslationInterface::class)->add('micro', '~/vendor/dynart/micro/translations');
;
; Then pass the translation to your forms via Form::setTranslation().
; Without it the forms fall back to these English texts.

form_required = "Required."
form_csrf_invalid = "CSRF token is invalid."
